/**
 * BACKLOG.md — Open feedback items from manual testing.
 * Harvested from mytesting.md (2026-09-14 testing session).
 * Items here are NOT wired yet — they are design/product decisions to address.
 */
# Backlog

Open items from manual testing. Not wired yet — design/product decisions pending.

---

## Overview Screen (dashboard/overview)

- [ ] **Remove setup checklist** — duplicates content already in sidebar (Set up profile → Settings, Create first item → My App, Customize your design → My App, Share your page → QR section below). Delete the entire checklist section.
- [ ] **Remove quick actions except "Edit My App"** — View my page is already in the sidebar, Install my fan app is already in the sidebar, Share your page is already in the QR section. Only "Edit My App" should remain. Move it to take the space where the setup checklist was.

## My App Editor — Identity Tab

- [ ] **Move Display Name to Identity tab** — currently in Design tab, user expects it in Identity alongside App Name. Move it.
- [ ] **App Icon needs upload button** — currently URL-only input. Add a file upload button (same pattern as catalog cover uploads).

## Admin Panel

- [ ] **Invite staff button broken** — clicking "Invite" in the staff invite popup does nothing (stays in the popup). Needs debugging.

## Search

- [ ] **Search should be accessible from Network tab** — user can't find `/search`. Add a search input to the Network page (left rail Discover section) so creators can search for other creators directly from the dashboard.
