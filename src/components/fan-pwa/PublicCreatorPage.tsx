/** PublicCreatorPage — NAKED. Old content in allScreens/fan-page/PublicCreatorPage-old.tsx. */
import type { CreatorBranding } from "@/lib/branding";
interface PublicCreatorPageProps { branding: CreatorBranding; }
export function PublicCreatorPage(_props: PublicCreatorPageProps) {
  return <div />;
}
